import React from "react";
import ReactDOM from "react-dom";
import MediaScrapper from "./MediaScrapper";
import "./index.scss";

ReactDOM.render(
  <React.StrictMode>
    <MediaScrapper />
  </React.StrictMode>,
  document.getElementById("root")
);
